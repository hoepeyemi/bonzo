# Environment Variables Documentation
# =====================================
# Each app has its own .env.example with detailed documentation.
# Copy the appropriate file to .env.local in each directory.

# apps/web/.env.local - Next.js app
# See: apps/web/.env.example

# apps/mcp-server/.env.local - MCP server
# See: apps/mcp-server/.env.example

# hardhat/.env - Smart contract deployment (optional)
# DEPLOYER_PRIVATE_KEY=0x...
# Optional: contract verification API key for Shannon explorer (if required by verify plugin)
# SOMNIA_EXPLORER_API_KEY=your_api_key
